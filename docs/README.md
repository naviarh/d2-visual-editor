# D2 visual editor — документация

Двусторонний редактор диаграмм: слева код [D2](https://d2lang.com), справа интерактивная схема. Любая правка кода применяется к схеме (после debounce ~3 c), а изменения схемы — обратно в код. Позиции элементов не теряются ни при переименовании, ни при переносе строк.

## Документы

| Документ | Что внутри |
|---|---|
| `docs/architecture.md` | Модель графа (источник истины), проекции «чистый/аннотированный», пайплайн правки текста, merge-эвристики, авто-расстановка, сортировка |
| `docs/development.md` | Запуск, тестирование, стиль кода, ловушки и ограничения для разработчика |
| `docs/plans/d2-editor-sync.md` | Исходный план реализации (этапы 1–7 выполнены) |
| `AGENTS.md` (корень) | Инструкции для ИИ-агентов: инварианты, семантика D2, правила работы |

## Быстрый старт

```bash
# открыть приложение (без сборки; нужен интернет для @xyflow/system с CDN)
xdg-open index.html

# юнит-тесты (106): node:test, без внешних зависимостей
npm test

# E2E (15 сценариев) — нужен системный Chrome
CHROME=/usr/bin/google-chrome-stable npm run test:ui
```

## Структура репозитория

```
index.html            весь UI (без сборки): split-панели «Код D2» и «Схема»; split-button «Копировать» с меню
                      (Вставить/Заменить/Импорт…/Экспорт…/Экспорт в SVG; Draw.io и Mermaid — стабы);
                      схема на @xyflow/system; dblclick-rename через модальный диалог
js/d2-serialize.js    граф → D2-текст (чистый и аннотированный)        [window.d2mod]
js/d2-parse.js        D2-текст → граф (токенизатор + parseD2)          [window.d2parse]
js/d2-merge.js        слияние распарсенного графа с текущим             [window.d2merge]
js/d2-layout.js       авто-расстановка новых нод                       [window.d2layout]
js/d2-sort.js         упорядочение блоков по стрелкам                  [window.d2sort]
test/*.test.mjs       юнит-тесты (node:test, createRequire) + E2E (puppeteer-core)
docs/                 эта документация + план
```

## Ключевые идеи

1. **Граф — источник истины** для хранения и структуры; D2-текст — проекция графа (см. `docs/architecture.md`).
2. **`order`** — единственный порядок эмиссии текста (id нод и рёбер вперемешку).
3. **Маркер `# @d2pos x,y`** — зарезервированный формат, хранит позицию в тексте. Переключается кнопкой «Скрыть/Показать позиции».
4. **Правка текста** (debounce 3 c) → parse → merge (перенос позиций) → render; правки схемы (drag и т.п.) → перегенерация текста, только если он «чист».
5. **Экспорт/импорт**: файлы D2 (нативный диалог сохранения, принудительное `.d2`, имя запоминается в `d2editor:ui:v1`), импорт из `.d2`/`.txt`, SVG-экспорт (самодостаточный векторный документ из графа); Draw.io и Mermaid — в плане (стабы в меню).
